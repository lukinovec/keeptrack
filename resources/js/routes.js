import Dashboard from './components/Dashboard';

export default {
    mode: 'history',
    routes: [
        {
            name: 'Dashboard',
            path: '/',
            component: Dashboard,
        }
    ]
}